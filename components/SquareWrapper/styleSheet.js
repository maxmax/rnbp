import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    // textAlign: 'center',
    justifyContent: 'center',
    padding: 20,
    margin: 20
  },
});
